# python的函数基本和js相同    使用def定义   参数也是有形参和实参
# 但是有一点，参数的位置可以变化 前提是使用age=20等这种写法 参数的位置能变化
# 缺省参数和js的基本相同，如果一个函数里面有默认参数 那么这个参数必须必须放在参数的最后面

#  函数一般有四类的参数
#  1、位置参数（和js一样）
#  2、关键字参数（和js类似就是可以有默认值，参数的顺序不固定）
#  3、缺省参数（位置是固定的可以设置默认值）
#  4、不定长参数（类似于js的arguement在python里面使用*args 是元组类型）

# 匿名函数定义 lambda 传入参数: 函数体(一行代码)    注意：这里的函数体只能写一行  不能写多行 参数不适用（）

# 匿名函数的定义  lambda